// This line imports the custom matchers from @testing-library/jest-dom.
// These matchers extend Vitest's expect API, allowing you to write more readable assertions
// like `expect(element).toBeInTheDocument()`.
import "@testing-library/jest-dom/";

// You can add other global test setup here if needed,
// e.g., mocking global browser APIs, polyfills, etc.
